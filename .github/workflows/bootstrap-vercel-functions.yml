name: Retired Vercel dependency bootstrap

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Dependency already committed
        run: echo "@vercel/functions is already pinned in package.json and package-lock.json; this bootstrap workflow is intentionally retired."
