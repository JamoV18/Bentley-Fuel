import SyncClient from "./SyncClient";
import "./sync.css";

export const metadata = {
  title: "921 Daily Sync · Falcon Fuel",
};

export default function Daily921SyncPage() {
  return <SyncClient />;
}
